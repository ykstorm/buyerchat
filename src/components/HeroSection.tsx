// Deprecated — replaced by Brand 2.0 minimalist landing in src/app/page.tsx.
// Kept as an empty module because the local sandbox does not permit file
// deletion. Safe to remove from the repo in a follow-up commit.
export {}
